namespace MyHi.Companion.Features.Shared;

/// <summary>One row on the home page: a lab instrument, not a product feature.</summary>
public sealed record NavDestination(string Title, string Subtitle, string Route);
